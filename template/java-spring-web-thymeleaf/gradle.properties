group=com.example

org.gradle.parallel=true
org.gradle.caching=false

# Adopt the configuration cache in warn mode first: report incompatibilities
# instead of failing the build while it converges.
org.gradle.configuration-cache=false
org.gradle.configuration-cache.problems=warn
