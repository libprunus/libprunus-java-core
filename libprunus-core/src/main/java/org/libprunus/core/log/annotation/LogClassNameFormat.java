package org.libprunus.core.log.annotation;

/** Class-name rendering format. */
public enum LogClassNameFormat {
    /** Uses simple class names. */
    SIMPLE,

    /** Uses fully-qualified class names. */
    FULLY_QUALIFIED
}
